<%= form_with(model: opinion, local: true) do |form| %>
  <% if opinion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opinion.errors.count, "error") %> prohibited this opinion from being saved:</h2>

      <ul>
        <% opinion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="talk ">
    <div class="d-flex bd-highlight align-items-center p-4">
      <div class="bd-highlight w-100">
        <%= form.label :body %>
        <%= form.text_area :body, class: " bd-highlight" %>
      </div>

      <div class="actions flex-shrink">
        <%= form.submit 'Talkey', class: "btn-blue w-100" %>
      </div>
    </div>
  </div>
<% end %>